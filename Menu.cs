namespace Evesoft
{
    public static class Menu
    {
        public const string root = "EveSoft";
        public const string utils = root + "/Utils";
        public const string input = root + "/Inputs";
        public const string views = root + "/Views";
        public const string localize = root + "/Localize";
    }
}